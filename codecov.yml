codecov:
  require_ci_to_pass: true

coverage:
  precision: 2
  round: down
  range: "40...100"
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 70%
        threshold: 5%

ignore:
  - "tests/**/*"
